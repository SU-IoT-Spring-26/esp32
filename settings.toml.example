# WiFi Configuration for CircuitPython
# Copy this file to settings.toml on the CIRCUITPY device and fill in your credentials

WIFI_SSID = "AirOrangeX"
# WIFI_PASSWORD = ""

# Unique ID for this sensor (used when uploading to API server). Set a different value per device.
# SENSOR_ID = "living-room"
# SENSOR_ID = "office"

# Using CIRCUITPY_WIFI_PASSWORD and SSID will autostart the web workflow
# we can save ~40KB of RAM by not autostarting it simply by using variables
# without the CIRCUITPY prefix

# Optional: Configure CircuitPython web workflow port (default is 80)
# CIRCUITPY_WEB_API_PORT = 80
# CIRCUITPY_WEB_API_PASSWORD = "your_password"
